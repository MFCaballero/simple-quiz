{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/internal/main.go",
            "args": ["question", "list"],
            "cwd": "${workspaceFolder}",
            "env": {
            }
        }
    ]
}